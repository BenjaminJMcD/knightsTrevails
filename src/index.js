import { convertToCoord } from "./bts";
import { convertToMatrix } from "./bts";
import { createAdjList } from "./bts";

// how so i create a binary search tree with 2 digit coords. 



console.log(convertToMatrix([1,2]));
console.log(convertToCoord(17));

console.log(createAdjList());

console.log(15%8)

function knightMoves(coord1, coord2) {
    
};